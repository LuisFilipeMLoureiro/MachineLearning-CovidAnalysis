# Machine Learning - Covid Analysis

### Instalando o projeto
`$ conda env create -f environment.yml`

`$ conda activate finalproject`

`$ poetry install`

### Trabalhando no projeto
`$ conda activate finalproject`
